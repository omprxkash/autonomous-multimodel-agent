# Decision Log — run-1

Every consequential call made during this run, with rationale. Newest at the bottom.

| # | Phase | Decision | Why |
|---|-------|----------|-----|
| 1 | 0 | Reconstructed the master prompt into `MASTER_PROMPT.md` from the user's pasted mission (the `/goal` command has a 4,000-char limit and rejected the full text). | Mission requires the instruction set to live in the project as the source of truth. |
| 2 | 0 | Video pipeline will be Playwright screen-recording + Windows System.Speech TTS + synthesized music + ffmpeg-static, because this workspace has **no** `.env`, no HeyGen/ElevenLabs keys, and no ffmpeg install. | Guardrail: no new spending, no sign-ups. Deviation from the reference video is disclosed in `honesty.md`. |
| 3 | 0 | Subagent model policy: Sonnet-class workers for research fan-out and builds; Opus-class only where judgment quality dominates (judge panels, red team synthesis). Orchestrator (Fable) plans, delegates, reviews. | Matches the mission's orchestration instruction while controlling cost. |
| 4 | 1 | Pain-hunt fan-out: 8 parallel researchers, each locked to a distinct source × angle lane, required to return dated, linked, quoted complaints from ~the last 12 months. | Independent lanes prevent groupthink and duplicate findings; dated quotes make the verification pass falsifiable. |
| 5 | 1 | Reddit and X were blocked at the crawler layer for several researchers. Accepted their documented workarounds (Arctic Shift Reddit-data mirror, old.reddit.com via Brave-discovered permalinks, Indie Hackers pivot) but flagged all workaround-sourced evidence for extra scrutiny in the verification pass. | Guardrail 3 (invent nothing) — evidence fetched through mirrors is real but easier to get wrong, so it gets adversarial re-checking rather than blanket trust. |
| 6 | 1 | Chargeback/dispute pain resurfaced organically as the loudest r/shopify complaint but was deliberately kept off the headline candidate list (noted as context only). | The mission is a fresh hunt; recycling the reference video's winner would make the tournament a formality. |
| 7 | 1a | Single synthesis agent merges all 8 lane files into a deduplicated master list (C01–C~20) with per-candidate confidence grades, before any verification or judging. | Verifiers and judges need one canonical, ID-addressable list; cross-lane merges concentrate corroboration signal instead of splitting it. |
| 8 | 1b/2 | Token-economy restructure (user asked mid-run for minimal token use): judge panel = 1 Opus agent role-playing 5 personas; adversarial verification deep-checks the 4 finalists only (not all 20); debates + final vote = 2 agents, not 8+. | Preserves the mission's tournament/skeptic structure where it changes the outcome (the finalists) while cutting ~80% of agent spend on candidates that can't win anyway. |
| 9 | 2 | **Winner: C09 — safety-first Shopify multi-store inventory sync** (dry-run, diff preview, rollback, circuit-breaker). Unanimous 3–0 fresh-judge vote after a 1-1-1 first round; C13 (failed-payment recovery) held as runner-up insurance. | Only finalist whose evidence *strengthened* under adversarial re-fetch; differentiated wedge no incumbent ships; proven durable WTP ($10–60/mo, multi-year retention despite failures). |
| 10 | 3/4 | Business design (Opus) and brand (Sonnet) launched in parallel rather than sequentially. | Wedge, audience, and emotional job (relief + control) are already fixed by the tournament record; parallelism saves wall-clock with no dependency violated. |
| 11 | 4 | Name: **ReconStock** (over SyncVault, CalmStock et al., 20 screened). Pricing: Free/$29/$49/$99, top of the verified $10–60 band. | Only candidate clean on trademark + .com/.io/.app simultaneously; "recon/reconcile" is the buyers' own vocabulary. Premium priced because Trunk proves the band tolerates $35–39 at 4.8★ and the differentiator is safety, not cost. |
| 12 | 5 | Builder launched before brand guidelines were finished (structure/data first, styling second); both build agents were later killed mid-run by the account's monthly spend limit and resumed from their transcripts rather than respawned. | Wall-clock economy; transcript resume preserves paid-for context instead of re-deriving it. |
| 13 | 7 | Red team ran early, in parallel with the build, against plan/research only; its 6 site/product fixes were handed to the builder afterward. Verdict: VIABLE-WITH-FIXES (33 attacks, 0 kills, 19 fixes applied, 14 accepted risks). Core messaging rule adopted: promise "fails safe and is reversible," never "never corrupts." | The plan/research attack surface didn't depend on the build; running early saved a full sequential phase. |
| 14 | 6 | Videos produced by resuming the builder agent (it already held the UI selectors and server setup) instead of briefing a fresh video agent. Launch video: on-screen copy + music, no VO; founder video: Windows TTS (Zira) voiceover, disclosed. | Context reuse ≫ cold start under a spend limit; the TTS voice and fictional founder are honesty-file disclosures, not hidden. |
| 15 | 8 | RECAP.html written inline by the orchestrator rather than delegated. | The orchestrator's context already contained every phase summary — the exact content the recap needs; delegation would have paid for re-reading the whole run. |
| 16 | 9 | Fresh-eyes audit: **PASS** — 45/45 linked files present, 34/34 checks re-passed by the auditor from the README, both videos decoded cleanly, RECAP rendered error-free. One blocker found (RECAP cited port 8787 vs. the server's actual 8123) and fixed, plus two wording minors fixed. | Definition of Done requires a stranger's walkthrough, not the builder's word; the audit caught exactly the kind of defect (copy-paste port) that self-review misses. |
| 17 | post | The finished run was re-graded clause-by-clause against the verbatim master prompt (`MASTER_PROMPT.md`) — results in `record/gap-analysis.md`. | The run had executed against a faithful reconstruction; the exact text contained requirements the reconstruction lacked. |
| 18 | post | Two gaps found and fixed: (a) mobile+desktop screenshot verification → shots 08–10 added, 0px mobile overflow; (b) extras beyond the floor → `extras/` added: investor teaser, onboarding email sequence, and the unexpected deliverable (`premortem.html`, a self-authored future 1-star review answered by engineering commitments). | The exact Definition of Done requires both; the premortem satisfies "invent at least one deliverable nobody would expect" while staying on the brand's honesty positioning. |
| 19 | post | Package published to github.com/omprxkash/autonomous-multimodel-agent (folder `company-builder-experiment/`) at the user's explicit direction, with the user's own git identity and no tool attribution; `_tools/node_modules` gitignored (README §0 covers reinstall). | The run-era "publish nothing" guardrail bound the autonomous run, not the owner; publishing is the user's decision, executed as instructed. |
